# =================================================================
# Configuration pour tester un backup restauré localement
# =================================================================
#
# 1. Faire un backup de prod/staging:
#    ./backend/scripts/backup_db.sh backend/.env.production
#
# 2. Restaurer dans un container de test:
#    ./backend/scripts/test_restore.sh backups/backup_XXXXXXXX_XXXXXX.dump
#
# 3. Lancer Django sur le backup restauré:
#    cd backend && source .env.test-restore && python manage.py runserver 8003
#
# 4. Nettoyer après:
#    docker rm -f pg_restore_test
#
# =================================================================

POSTGRES_HOST=localhost
POSTGRES_PORT=5555
POSTGRES_DB=railway
POSTGRES_USER=postgres
POSTGRES_PASSWORD=testpassword
