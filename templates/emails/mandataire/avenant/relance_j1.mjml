<mjml>
  {% include "emails/components/head.mjml" %}

  <mj-body background-color="#f5f7f9" css-class="body-wrapper">
    {% include "emails/components/header.mjml" %}

    <mj-section background-color="white" border-radius="8px" padding="20px 15px" css-class="content-card">
      <mj-column>
        <mj-text font-size="22px" font-weight="700">Informations manquantes — complétez le bail 📝</mj-text>

        <mj-text>Bonjour {{ prenom }},</mj-text>

        <mj-text>Lors de la création du bail du logement situé au <strong>{{ adresse }}</strong>, certaines informations obligatoires n'ont pas été renseignées :</mj-text>

        <mj-text>
          {% if manque_ddt %}• Le <strong>Dossier de Diagnostics Techniques (DDT)</strong>, incluant notamment le DPE<br/>{% endif %}
          {% if manque_numero_fiscal %}• Le <strong>numéro fiscal du logement</strong> — nécessaire pour les déclarations{% endif %}
        </mj-text>

        <mj-text>
          <table cellpadding="0" cellspacing="0" border="0" width="100%" style="background-color: #fff3e0; border-radius: 8px;" class="box-warning">
            <tr>
              <td style="padding: 20px; font-size: 16px;" class="box-warning">
                <span style="font-weight: 600; color: #e65100;" class="text-warning">👉 Ces éléments sont légalement obligatoires :</span><br/><br/>
                {% if manque_ddt %}• Le DDT doit être annexé à tout bail (loi du 6 juillet 1989, décret n°2006-1147)<br/>{% endif %}
                {% if manque_numero_fiscal %}• Le numéro fiscal doit figurer dans le bail (loi de finances 2020, art. 1418){% endif %}
              </td>
            </tr>
          </table>
        </mj-text>

        <mj-text>Pour sécuriser le bailleur et informer correctement le locataire, il est nécessaire de générer <strong>un avenant</strong> afin de régulariser le contrat.</mj-text>

        <mj-text>Hestia vous permet de compléter ces informations et de produire l'avenant en quelques clics.</mj-text>

        <mj-button href="{{ cta_url }}" css-class="btn-primary">Compléter le bail et générer l'avenant</mj-button>

        <mj-text>Nous restons disponibles pour vous accompagner dans cette démarche.</mj-text>

        {% include "emails/components/signature.mjml" %}
      </mj-column>
    </mj-section>

    {% include "emails/components/footer.mjml" %}
  </mj-body>
</mjml>
