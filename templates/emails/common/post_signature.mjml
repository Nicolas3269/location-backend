<mjml>
  {% include "emails/components/head.mjml" %}
  {% load french_grammar %}

  <mj-body background-color="#f5f7f9" css-class="body-wrapper">
    {% include "emails/components/header.mjml" %}

    <mj-section background-color="white" border-radius="8px" padding="20px 15px" css-class="content-card">
      <mj-column>
        <mj-text font-size="22px" font-weight="700">Votre signature est bien enregistrée ✔️</mj-text>

        <mj-text>Bonjour {{ prenom }},</mj-text>

        <mj-text>Votre signature{% if role == 'mandataire' %} en tant que mandataire{% elif role == 'bailleur' %} en tant que bailleur{% endif %} a bien été prise en compte — merci !</mj-text>

        <mj-text font-weight="600" padding-bottom="5px">Avancement des signatures :</mj-text>

        <!-- Liste des signataires -->
        <mj-text padding-top="0">{% for signataire in signataires_signes %}<span style="color: #479965">✓</span> {{ signataire.nom }}{% if signataire.date %} <span style="color: #6c6b70">({{ signataire.date }})</span>{% endif %}<br/>{% endfor %}{% if signataires_restants %}{% for signataire in signataires_restants %}<span style="color: #d0ccc5">○</span> {{ signataire.nom }} <span style="color: #6c6b70">(en attente)</span><br/>{% endfor %}{% endif %}</mj-text>

        {% if prochain_signataire %}
        <mj-text>
          <table cellpadding="0" cellspacing="0" border="0" width="100%" style="background-color: #e8f4fd; border-radius: 8px;" class="box-info">
            <tr>
              <td style="padding: 15px;" class="box-info">
                <span style="color: #2680eb;">👉 Prochain signataire : <strong>{{ prochain_signataire.nom }}</strong></span><br/>
                <span style="color: #6c6b70;">Un lien sécurisé vient de lui être envoyé automatiquement.</span>
              </td>
            </tr>
          </table>
        </mj-text>
        {% endif %}

        <mj-divider border-color="#d0ccc5" css-class="divider" />

        <mj-text>Vous pouvez à tout moment :</mj-text>

        <mj-text padding-top="0">
          • Télécharger le document actuel (version provisoire)<br/>
          • Suivre l'avancement des signatures en temps réel{% if role == 'bailleur' or role == 'mandataire' %}<br/>
          • Retrouver tous vos documents{% endif %}
        </mj-text>

        <mj-button href="{{ lien_espace }}" css-class="btn-primary">Accéder à mon espace</mj-button>

        <mj-text>{{ document_type|avec_le|capfirst }} ne sera juridiquement valable qu'une fois toutes les signatures apposées. Vous recevrez un email de confirmation dès que la procédure sera finalisée.</mj-text>

        {% include "emails/components/signature.mjml" %}
      </mj-column>
    </mj-section>

    {% include "emails/components/footer.mjml" %}
  </mj-body>
</mjml>
