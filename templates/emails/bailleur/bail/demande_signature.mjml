<mjml>
  {% include "emails/components/head.mjml" %}

  <mj-body background-color="#f5f7f9" css-class="body-wrapper">
    {% include "emails/components/header.mjml" %}

    <mj-section background-color="white" border-radius="8px" padding="20px 15px" css-class="content-card">
      <mj-column>
        <mj-text font-size="22px" font-weight="700">Votre signature est requise pour le {{ document_type }} ✍️</mj-text>

        <mj-text>Bonjour {{ prenom }},</mj-text>

        <mj-text>Vous êtes invité(e) à <strong>signer le {{ document_type }}</strong> concernant le logement situé au <strong>{{ adresse_logement }}</strong>.</mj-text>

        <mj-button href="{{ signature_url }}" css-class="btn-primary">Signer le {{ document_type }}</mj-button>

        <mj-text>La signature est <strong>sécurisée</strong>, <strong>personnelle</strong> et ne prend que <strong>2 minutes</strong>.</mj-text>

        <mj-text>
          <table cellpadding="0" cellspacing="0" border="0" width="100%" style="background-color: #f5f7f9; border-radius: 8px;" class="box-info">
            <tr>
              <td style="padding: 20px; font-size: 16px;" class="box-info">
                <strong>Une fois votre signature apposée :</strong><br/><br/>
                ✓ le document sera transmis au signataire suivant (s'il y en a)<br/>
                ✓ vous recevrez un email de confirmation<br/>
                ✓ le {{ document_type }} complet sera disponible dans votre espace personnel
              </td>
            </tr>
          </table>
        </mj-text>

        <mj-text>👉 <a href="{{ lien_espace }}" style="color: #2680eb;">Accéder à votre espace</a></mj-text>

        <mj-text>Si vous avez des questions, nous sommes là pour vous aider.</mj-text>

        {% include "emails/components/signature.mjml" %}
      </mj-column>
    </mj-section>

    {% include "emails/components/footer.mjml" %}
  </mj-body>
</mjml>
